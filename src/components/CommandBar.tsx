import * as React from 'react';
import {ReactElement} from 'react';

export const CommandBar: React.FunctionComponent<{}> = (): ReactElement => {
  return (
      <div className={'command-bar'}>Command Bar</div>
  );
};
